name: "Modern Full-stack React Projects"
services:
  database:
    image: mongodb/mongodb-community-server:8.0-ubi9-slim
    ports:
      - "10000:27017"
    volumes:
      - db-data:/data/db

volumes:
  db-data:
